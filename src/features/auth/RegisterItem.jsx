// import InputForm from "../../components/InputForm";

// export default function RegisterItem({ items }) {
//   const itemlist = items.map((item, index) => (
//     <InputForm key={index} placeholder={item.placeholder} onChange={item.onChange} />
//   ));

//   return <div>{itemlist}</div>;
// }
