// import Loading from "./component/Loading";
import Router from "./router/Router";

function App() {
  // const { initialLoading } = useAuth();
  // if (initialLoading) {
  //   return <Loading />
  // }
  return (
    <div>
      <Router />
    </div>
  );
}

export default App;
